<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <supports-screens android:resizeable="true"
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true"/>

    <!-- Removido <compatible-screens>: era uma lista fixa de combinações (tamanho, densidade)
         de tela, e a Play Store trata qualquer dispositivo fora dela como "incompatível" -
         mesmo que o app rode perfeitamente nele (supports-screens acima já cobre qualquer
         tamanho/densidade). A lista não incluía densidades comuns em aparelhos Motorola
         (ex.: 400/440dpi, entre outras não listadas), o que os deixava marcados como
         incompatíveis na Play Store mesmo sem nenhum problema real de compatibilidade.
         O próprio Google recomenda não usar esse elemento por causa desse risco. -->

    <application
        android:allowBackup="true"
        android:requestLegacyExternalStorage="true"
        android:icon="@drawable/pomba"
        android:label="@string/app_name"
        android:roundIcon="@drawable/pomba"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="app.convencao.cadier.view.activity.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            tools:replace="android:label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="app.convencao.cadier.view.activity.MenuActivity"/>
        <activity android:name="app.convencao.cadier.view.activity.ProfileEditActivity"/>
        <activity android:name="app.convencao.cadier.view.activity.AddressEditActivity"/>
    </application>

</manifest>
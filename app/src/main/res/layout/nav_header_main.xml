<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:background="@drawable/side_nav_bar"
    android:gravity="bottom"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <FrameLayout
        android:layout_width="68dp"
        android:layout_height="68dp">

        <View
            android:layout_width="68dp"
            android:layout_height="68dp"
            android:background="@drawable/bg_cadier_avatar_ring_white"/>

        <ImageView
            android:id="@+id/imageViewMenu"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_gravity="center"
            android:background="@drawable/bg_cadier_avatar_mask"
            android:clipToOutline="true"
            android:scaleType="centerInside"/>
    </FrameLayout>

    <TextView
        android:id="@+id/txtNomeMenu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        android:text="Nome"
        android:textColor="@color/cadier_text_on_dark"
        android:textSize="@dimen/cadier_text_subtitle"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/txtIgrejaMenu"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Igreja"
        android:textColor="@color/cadier_teal_light_bg"
        android:textSize="@dimen/cadier_text_body_secondary" />

</LinearLayout>